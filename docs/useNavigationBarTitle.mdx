---
title: useNavigationBarTitle
---

动态更新导航栏属性.

:::tip 提示
只有当前页面显示时才会更新导航栏标题
:::

## 用法

```jsx
import { useNavigationBarTitle } from '@tarojsx/hooks'

const Demo = ({ article }) => {
    useNavigationBarTitle(article.title)
}
```

## 参考

```ts
useNavigationBarTitle(title)
```
